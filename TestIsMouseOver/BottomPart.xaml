<UserControl
    x:Class="TestIsMouseOver.BottomPart"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:TestIsMouseOver"
    mc:Ignorable="d" 
    d:DesignWidth="800" d:DesignHeight="450"
    x:Name="Root"
>
    <UserControl.DataContext>
        <local:BottomPartViewModel />
    </UserControl.DataContext>
    <Border Background="Transparent">
        <Grid>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock>This is the bottom part.</TextBlock>
                <TextBlock Text="{Binding ElementName=Root, Path=IsMouseOver, StringFormat={}IsMouseOver is {0}.}"/>
            </StackPanel>
            <ItemsControl HorizontalAlignment="Left" VerticalAlignment="Top" ItemsSource="{Binding Items}" />
        </Grid>
    </Border>
</UserControl>
