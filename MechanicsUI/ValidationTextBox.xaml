<UserControl
    x:Class="MechanicsUI.ValidationTextBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:MechanicsUI"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=local:DesignValidationTextBoxViewModel, IsDesignTimeCreatable=True}"
>
    <UserControl.Resources>
        <local:HasErrorToBrushConverter x:Key="borderBrushConverter" />
    </UserControl.Resources>
    <TextBox
        BorderThickness="2"
        BorderBrush="{Binding HasError, Converter={StaticResource borderBrushConverter}}"
        ToolTip="{Binding TooltipText}"
        Text="{Binding TextboxText, Mode=TwoWay}"
    />
</UserControl>
