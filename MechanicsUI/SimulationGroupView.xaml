<UserControl
    x:Class="MechanicsUI.SimulationGroupView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:MechanicsUI"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=local:SimulationGroupVM, IsDesignTimeCreatable=False}"
>
    <ItemsControl HorizontalAlignment="Left" VerticalAlignment="Top" ItemsSource="{Binding SimulationNames}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Button
                    HorizontalAlignment="Left"
                    Content="{Binding}"
                    Click="Button_Click"
                />
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
