<UserControl
    x:Class="GuiByReflection.Views.UserEntryViews.NullableUserEntryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:GuiByReflection.Views"
    xmlns:designInstances="clr-namespace:GuiByReflection.Views.DesignInstances"
    mc:Ignorable="d" 
    d:DataContext="{d:DesignInstance Type=designInstances:DesignNullableIntEntryVM, IsDesignTimeCreatable=True}"
>
    <UserControl.Resources>
        <local:UserEntryDataTemplateSelector x:Key="entryTemplateSelector" />
    </UserControl.Resources>
    <StackPanel Orientation="Horizontal">
        <ContentControl Content="{Binding HasValueVM}" ContentTemplateSelector="{StaticResource entryTemplateSelector}" />
        <ContentControl Content="{Binding UnderlyingValueVM}" ContentTemplateSelector="{StaticResource entryTemplateSelector}" />
    </StackPanel>
</UserControl>
