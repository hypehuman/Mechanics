<UserControl
    x:Class="GuiByReflection.Views.ExceptionButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="clr-namespace:GuiByReflection.Views"
    xmlns:designInstances="clr-namespace:GuiByReflection.Views.DesignInstances"
    mc:Ignorable="d" 
    d:DesignWidth="200" d:DesignHeight="40"
    d:DataContext="{d:DesignInstance Type=designInstances:DesignButtonExceptionVM, IsDesignTimeCreatable=True}"
>
    <UserControl.Resources>
        <views:CollapsedIfNullOrWhitespaceConverter x:Key="visibleIfThere" />
    </UserControl.Resources>
    <Button
        Visibility="{Binding Converter={StaticResource visibleIfThere}}"
        ToolTip="{Binding}"
        Click="Button_Click"
        Background="#44FF4400"
    >
        <!-- The transparent background brush works well with both black-on-white and white-on-black themes. -->
        <TextBlock TextWrapping="Wrap" TextTrimming="CharacterEllipsis" Text="{Binding ShortMessage, StringFormat='{}Error (click to copy): {0}'}" />
    </Button>
</UserControl>
